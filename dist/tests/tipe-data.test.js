"use strict";
describe('Data Type', function () {
    it('should must declar', function () {
        let name = "Tantowi Shah Hanif";
        let balance = 1000;
        let isVip = true;
        console.log(name);
        console.log(balance);
        console.log(isVip);
        // name = 1
        // balance = "10000"
        // isVip = 1
    });
});
