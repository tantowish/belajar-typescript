"use strict";
describe('Data Type', function () {
    it('should must declar', function () {
        let name = "Tantowi Shah Hanif";
        const balace = 1000;
        const isVip = true;
        console.log(name);
        console.log(balace);
        console.log(isVip);
    });
});
